models:
  - name: fancy_delimiters_regular_inside
    description: "This description uses fancy quotes as delimiters with regular \"double quotes\" inside the value."
    config:
      materialized: view
      meta:
        custom_config_1: some_value
  - name: regular_delimiters_fancy_inside
    description: "This description uses regular delimiters with fancy “double quotes” inside the value."
    config:
      materialized: view
      meta:
        custom_config_2: some_value
  - name: unquoted_with_fancy_inside
    description: This description is unquoted with fancy “double quotes” inside the value.
    config:
      materialized: view
      meta:
        custom_config_3: some_value
  - name: regular_delimiters_escaped_inside
    description: "This description uses regular delimiters with escaped \"double quotes\" inside the value."
    config:
      materialized: view
      meta:
        custom_config_4: some_value
