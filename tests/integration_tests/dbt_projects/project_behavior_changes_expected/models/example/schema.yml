# models
models: # models - inline
    # models.[0].name
  - name: model_with_spaces # models.[0].name - inline
    # models.[1].name
  - name: python_model_with_spaces # models.[1].name - inline

# exposures
exposures: # exposures - inline
    # exposures.[0].name
  - name: exposure_with_spaces # exposures.[0].name - inline