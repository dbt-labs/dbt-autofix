name: my_project
version: "1.0.0"
profile: my_project
model-paths: ["models"]
clean-targets:
  - target
  - dbt_packages
data_tests:
  +store_failures: true
models:
  my_project:
    +materialized: table
    incr:
      +incremental_predicates: ["a = b"]